spec:
  name: durov
  services:
    - name: backend
      dockerfile_path: Dockerfile
      github:
        repo: demidko/durov
        branch: main
        deploy_on_push: true
      envs:
        - key: TOKEN
          type: SECRET
        - key: CHANNEL
          type: SECRET
